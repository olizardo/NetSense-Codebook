# Bluetooth to EgoID Mapping

This table maps the raw Bluetooth hardware pseudonyms to the standardized `egoid` identifiers.

```{r}
#| echo: false
#| message: false
library(readr)
library(knitr)
df <- read_csv("BT_egoid_mapping.csv", show_col_types = FALSE)
kable(df)
```
